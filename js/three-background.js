const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,0.1,1000);const renderer=new THREE.WebGLRenderer();renderer.setSize(window.innerWidth,window.innerHeight);document.body.appendChild(renderer.domElement);const textureLoader=new THREE.TextureLoader();const grecaTexture=textureLoader.load("https://i.imgur.com/GrecaTexture.png");const bgPlane=new THREE.Mesh(new THREE.PlaneGeometry(200,200),new THREE.MeshBasicMaterial({map:grecaTexture,opacity:0.2,transparent:true}));bgPlane.position.z=-50;scene.add(bgPlane);function createTattooMachine(x,y,z){const body=new THREE.Mesh(new THREE.BoxGeometry(2,1,1),new THREE.MeshBasicMaterial({color:0x444444}));body.position.set(x,y,z);scene.add(body);}function createNeedle(x,y,z){const needle=new THREE.Mesh(new THREE.CylinderGeometry(0.05,0.05,2,8),new THREE.MeshBasicMaterial({color:0xaaaaaa}));needle.position.set(x,y,z);scene.add(needle);}for(let i=0;i<5;i++){createTattooMachine(Math.random()*20-10,Math.random()*20-10,Math.random()*-20);createNeedle(Math.random()*20-10,Math.random()*20-10,Math.random()*-20);}camera.position.z=30;function animate(){requestAnimationFrame(animate);scene.rotation.y+=0.001;renderer.render(scene,camera);}animate();
