const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,0.1,1000);const renderer=new THREE.WebGLRenderer();renderer.setSize(window.innerWidth,window.innerHeight);document.body.appendChild(renderer.domElement);const light=new THREE.AmbientLight(0xffffff,1);scene.add(light);function createMachine(x,y,z){const body=new THREE.Mesh(new THREE.BoxGeometry(2,1,1),new THREE.MeshBasicMaterial({color:0x00ff00}));body.position.set(x,y,z);scene.add(body);}function createNeedle(x,y,z){const needle=new THREE.Mesh(new THREE.CylinderGeometry(0.1,0.1,3,12),new THREE.MeshBasicMaterial({color:0xffff00}));needle.position.set(x,y,z);scene.add(needle);}for(let i=0;i<8;i++){createMachine(Math.random()*10-5,Math.random()*10-5,Math.random()*-10);createNeedle(Math.random()*10-5,Math.random()*10-5,Math.random()*-10);}camera.position.z=20;function animate(){requestAnimationFrame(animate);scene.rotation.y+=0.001;renderer.render(scene,camera);}animate();
