const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,0.1,1000);const renderer=new THREE.WebGLRenderer();renderer.setSize(window.innerWidth,window.innerHeight);document.body.appendChild(renderer.domElement);const light=new THREE.AmbientLight(0xffffff,1.2);scene.add(light);const geometry=new THREE.IcosahedronGeometry(8,2);const material=new THREE.MeshStandardMaterial({color:0x00ccff,wireframe:true,opacity:0.3,transparent:true});const gyroscope=new THREE.Mesh(geometry,material);scene.add(gyroscope);camera.position.z=25;function animate(){requestAnimationFrame(animate);gyroscope.rotation.x+=0.003;gyroscope.rotation.y+=0.004;scene.rotation.y+=0.0005;renderer.render(scene,camera);}animate();
