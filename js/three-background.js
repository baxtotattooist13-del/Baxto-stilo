const scene=new THREE.Scene();const camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,0.1,1000);const renderer=new THREE.WebGLRenderer();renderer.setSize(window.innerWidth,window.innerHeight);document.body.appendChild(renderer.domElement);function createMachine(x,y,z){const body=new THREE.Mesh(new THREE.BoxGeometry(1.2,0.6,0.6),new THREE.MeshBasicMaterial({color:0x00ff66,opacity:0.6,transparent:true}));body.position.set(x,y,z);scene.add(body);}function createNeedle(x,y,z){const needle=new THREE.Mesh(new THREE.CylinderGeometry(0.05,0.05,2,8),new THREE.MeshBasicMaterial({color:0xffff33,opacity:0.5,transparent:true}));needle.position.set(x,y,z);scene.add(needle);}for(let i=0;i<6;i++){createMachine(Math.random()*20-10,Math.random()*20-10,Math.random()*-20);createNeedle(Math.random()*20-10,Math.random()*20-10,Math.random()*-20);}camera.position.z=25;function animate(){requestAnimationFrame(animate);scene.rotation.y+=0.001;renderer.render(scene,camera);}animate();
